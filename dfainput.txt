Program circle ( input , output ) ;   (*program circle ();  *)
const
PI = 3.141592654 ;

var
r , d , c : real ;   (*variable declaration: radius, dia, circumference*)
    (* for LPas, split into 3 single declarations *)

begin
   writeln ( ' Enter the radius of the circle ' ) ;
   
   d := 2 * r ;
   c :=  PI * d ;
.
